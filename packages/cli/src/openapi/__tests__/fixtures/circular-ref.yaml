openapi: "3.0.0"
info:
  title: Circular Ref API
  version: "1.0.0"
paths:
  /nodes:
    get:
      summary: List nodes
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Node'
components:
  schemas:
    Node:
      type: object
      properties:
        child:
          $ref: '#/components/schemas/Node'
